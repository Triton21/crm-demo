{% extends "AppBundle:Marketing:marketingbase.html.twig" %}
{% block title %}Welcome!{% endblock %}

{% block welcome %}Welcome {{name}}{% endblock %}



{%block content %}  
    <div class="container">

        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">Saved Newsletter templates</h3>
                </div>
                <div class="panel-body">
                    <div class="gi-1x">
                        <table class="table table-striped gi-5p">

                            <thead>
                                <tr>
                                    <th>Template name</th>
                                    <th>Subject</th>
                                    <th>Created at</th>
                                    <th>Created by</th>
                                    <th>Edit</th>
                                    <th>Open</th>
                                    <th></th>
                                    <th></th>

                                </tr>
                            </thead>
                            {% for all in etemplist %}
                                <tr>
                                    <td style="width:10%">{{ all.tempname }}</td>
                                    <td style="width:10%">{{ all.subject }}</td>
                                    <td style="width:10%">{{ all.updatedAt|date('m-d-Y') }}</td>
                                    <td style="width:10%">{{all.username}}</td>
                                    <td style="width:5%"><a href="{{ path('marketing_edit', { 'id': all.id }) }}"><span class="glyphicon glyphicon-edit"></span></a></td>
                                    <td style="width:5%"><a href="{{ path('marketing_displaytemplate', { 'id': all.id }) }}" target="_blank"><span class="glyphicon glyphicon-open"></span></a></td>
                                    <td style="width:10%"><a href="{{ path('marketing_duplicate', { 'id': all.id }) }}" class="btn btn-info btn-xs">Duplicate</a></td>
                                    <td style="width:10%">{% if all.username == name %}<a href="{{ path('marketing_delete', { 'id': all.id }) }}" class="btn btn-danger btn-xs">Delete</a>{% endif %}</td>
                                {% endfor %}
                            </tr>
                        </table>

                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">Create new template </h3>
                </div>
                <div class="panel-body">
                   
                    
                   <p>
                        {{ form_start(form) }}</p>
                    <p>
                        {{ form_label(form.tempname) }}
                        {{ form_errors(form.tempname) }}
                        {{ form_widget(form.tempname) }}
                    </p>
                    <p>
                        {{ form_label(form.subject) }}
                        {{ form_errors(form.subject) }}
                        {{ form_widget(form.subject) }}
                    </p>
                    
                    <p>
                        
                        {{ form_errors(form.body) }}
                        {{ form_widget(form.body , { 'attr': { 
                            'style' : 'height:250px', 
                            'rows' : '30', 
                            'cols' : '1' }} ) }}
                    </p>
                     <p>
                        {{ form_end(form) }}</p>
                    


                </div>
            </div>
        </div> 
                      






    </div>

{%endblock%}   



{% block emailform %}
{% endblock %}




{% block javascripts %}{% endblock %}
