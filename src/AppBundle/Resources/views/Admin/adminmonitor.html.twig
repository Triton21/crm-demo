{% extends "AppBundle:Admin:adminbase.html.twig" %}
{% block title %}Welcome!{% endblock %}

{% block welcome %}Welcome {{name}}{% endblock %}



{%block content %} 
    <div class="col-md-12">
        <br>
        <div class="col-md-offset-4 col-md-2">
            <form class="form-inline" action="{{ path('admin_monitor')}}" method="POST">
                <input type="text" name="selectdate" id="datepicker" class="input-sm form-control" data-provide="datepicker" data-date-format="dd-mm-yyyy" placeholder="Select a date" data-date-format="dd-mm-yyyy" >
                <button type="submit" class="btn btn-default btn-sm">Select</button>
            </form>
        </div>
        <div class="col-md-1">
            &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<a href="{{ path('admin_monitor')}}"><button type="button" class="btn btn-success btn-sm">Today</button></a>
        </div>
        <br>
        <br>
        <br>
    </div>
    <div class="col-md-11">
        <div class="col-xs-1" style="text-align: right;">
            <div style="min-height: 100px;"></div>
            <a href="{{ path('admin_monitor', { 'referencedate': nowDateString, 'pager': 'down'})}}"><span class="gi-45p glyphicon glyphicon-chevron-left"></span></a>
        </div>
        <div class="col-xs-2">
            <div   {% if (date5|date('l') == 'Saturday') or (date5|date('l') == 'Sunday') %}class="panel panel-danger"{% else %}class="panel panel-info"{% endif %}>
                <div class="panel-heading" style="text-align: center;">
                    <h3 class="panel-title">{{ date5|date('d-m-Y') }}&nbsp;&nbsp;{{ date5|date('l') }} </h3>
                </div>
                <div class="panel-body">

                    <table class="table table-striped gi-5p">

                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Type</th>
                                <th>Time</th>
                            </tr>
                        </thead>
                        {% for all in usermonitor5 %}
                            <tr>
                                <td style="width:33.3%">{{ all.username }}</td>
                                <td style="width:33.3%">{{ all.logtype }}</td>
                                <td style="width:33.3%">{{ all.createdAt|date('H:i:s') }}</td>
                            {% endfor %}
                        </tr>
                    </table>
                </div>  
            </div>
        </div>
        <div class="col-xs-2">
            <div   {% if (date4|date('l') == 'Saturday') or (date4|date('l') == 'Sunday') %}class="panel panel-danger"{% else %}class="panel panel-info"{% endif %}>
                <div class="panel-heading" style="text-align: center;">
                    <h3 class="panel-title">{{ date4|date('d-m-Y') }}&nbsp;&nbsp;{{ date4|date('l') }} </h3>
                </div>
                <div class="panel-body">

                    <table class="table table-striped gi-5p">

                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Type</th>
                                <th>Time</th>
                            </tr>
                        </thead>
                        {% for all in usermonitor4 %}
                            <tr>
                                <td style="width:33.3%">{{ all.username }}</td>
                                <td style="width:33.3%">{{ all.logtype }}</td>
                                <td style="width:33.3%">{{ all.createdAt|date('H:i:s') }}</td>
                            {% endfor %}
                        </tr>
                    </table>
                </div>  
            </div>
        </div>
        <div class="col-xs-2">
            <div {% if (date3|date('l') == 'Saturday') or (date3|date('l') == 'Sunday') %}class="panel panel-danger"{% else %}class="panel panel-info"{% endif %}>
                <div class="panel-heading" style="text-align: center;">
                    <h3 class="panel-title">{{ date3|date('d-m-Y') }}&nbsp;&nbsp;{{ date3|date('l') }} </h3>
                </div>
                <div class="panel-body">

                    <table class="table table-striped gi-5p">

                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Type</th>
                                <th>Time</th>
                            </tr>
                        </thead>
                        {% for all in usermonitor3 %}
                            <tr>
                                <td style="width:33.3%">{{ all.username }}</td>
                                <td style="width:33.3%">{{ all.logtype }}</td>
                                <td style="width:33.3%">{{ all.createdAt|date('H:i:s') }}</td>
                            {% endfor %}
                        </tr>
                    </table>
                </div>  
            </div>
        </div>
        <div class="col-xs-2">
            <div   {% if (date2|date('l') == 'Saturday') or (date2|date('l') == 'Sunday') %}class="panel panel-danger"{% else %}class="panel panel-info"{% endif %}>
                <div class="panel-heading" style="text-align: center;">
                    <h3 class="panel-title">{{ date2|date('d-m-Y') }}&nbsp;&nbsp;{{ date2|date('l') }} </h3>
                </div>
                <div class="panel-body">

                    <table class="table table-striped gi-5p">

                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Type</th>
                                <th>Time</th>
                            </tr>
                        </thead>
                        {% for all in usermonitor2 %}
                            <tr>
                                <td style="width:33.3%">{{ all.username }}</td>
                                <td style="width:33.3%">{{ all.logtype }}</td>
                                <td style="width:33.3%">{{ all.createdAt|date('H:i:s') }}</td>
                            {% endfor %}
                        </tr>
                    </table>
                </div>  
            </div>
        </div>
        <div class="col-xs-2">
            <div   {% if (date1start|date('l') == 'Saturday') or (date1start|date('l') == 'Sunday') %}class="panel panel-danger"{% else %}class="panel panel-info"{% endif %}>
                <div class="panel-heading" style="text-align: center;">
                    <h3 class="panel-title">{{ date1start|date('d-m-Y') }}&nbsp;&nbsp;{{ date1start|date('l') }}</h3>
                </div>
                <div class="panel-body">

                    <table class="table table-striped gi-5p">

                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Type</th>
                                <th>Time</th>
                            </tr>
                        </thead>
                        {% for all in usermonitor1 %}
                            <tr>
                                <td style="width:33.3%">{{ all.username }}</td>
                                <td style="width:33.3%">{{ all.logtype }}</td>
                                <td style="width:33.3%">{{ all.createdAt|date('H:i:s') }}</td>
                            {% endfor %}
                        </tr>
                    </table>
                </div>  
            </div>
        </div>
        <div class="col-xs-1">
            <div style="min-height: 100px;"></div>
            {% if todayString == nowDateString %}<span class="gi-45p glyphicon glyphicon-chevron-right"></span>{% else %}<a href="{{ path('admin_monitor', { 'referencedate': nowDateString, 'pager': 'up'})}}"><span class="gi-45p glyphicon glyphicon-chevron-right"></span></a>{% endif %}
        </div>



    </div>


{%endblock%}   




{% block javascripts %}






{% endblock %}