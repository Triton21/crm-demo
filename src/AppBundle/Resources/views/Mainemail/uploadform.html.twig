<div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-body">

                    
                    <form action="{{ path('mainemail_ajaxformupload', { 'settid' : settid, 'emailid' : emailid}) }}" id="formnewmessageupload" method="post" {{ form_enctype(form) }}> 
                        <div class="form-group">
                        <label for="exampleInputFile"><span class="gi-6p glyphicon glyphicon-paperclip"></span> Attachment</label>
                        {{ form_widget(form.imageFile) }}
                        <p class="help-block">Accepted files: pdf, jpg, xls, xlxs, doc, docx</p>
                        </div>
                        <button type="submit" class="btn btn-default">Upload</button>
                    </form>

                </div>
            </div>
        </div>            